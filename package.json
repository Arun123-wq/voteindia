{
  "name": "voteindia-fullstack",
  "version": "1.0.0",
  "description": "Unified build for VoteIndia",
  "scripts": {
    "build": "cd voteindia-vite && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/server.js"
  }
}
